import { TestBed } from '@angular/core/testing';

import { SynchronizeTopMatchService } from './synchronize-top-match.service';

describe('SynchronizeTopMatchService', () => {
  let service: SynchronizeTopMatchService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(SynchronizeTopMatchService);
  });

  // it('should be created', () => {
  //   expect(service).toBeTruthy();
  // });
});
